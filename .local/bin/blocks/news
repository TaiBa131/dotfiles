#!/usr/bin/env sh

case $BLOCK_BUTTON in
        1) $TERMINAL -e newsboat & ;;
esac
pgrep newsboat > /dev/null || newsboat -x print-unread | awk '{print $1}' | sed s/^$//g > /tmp/newsnumber
news=$(cat /tmp/newsnumber)
if [ $news != 0 ]; then
	echo " ${news}"
fi
