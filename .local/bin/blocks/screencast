#!/usr/bin/env sh
[ -z "$BLOCK_BUTTON" ] && pgrep wf-recorder > /dev/null && echo ''
case $BLOCK_BUTTON in
    1) pkill --signal=INT wf-recorder && notify-send ' Killed Screencast';;
    3) pkill --signal=INT wf-recorder && notify-send ' Killed Screencast';;
esac
