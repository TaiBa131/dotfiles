#!/usr/bin/env sh
date '+ %d-%m-%Y  %H:%M'
case $BLOCK_BUTTON in
    1) i3-msg -q "exec $TERMINAL -e calcurse && savetodownloadsongs.sh" ;;
    3) i3-msg -q -- exec $TERMINAL -e tty-clock -s -c -b ;;
esac
